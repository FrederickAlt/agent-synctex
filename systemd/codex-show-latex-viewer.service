[Unit]
# DEPRECATED: legacy Python viewer-service unit kept for compatibility only.
# Production startup uses `npm run host-service:start` (scripts/agent-synctex-host-service.ts).
Description=Codex Show LaTeX legacy Python viewer service (deprecated)
After=graphical-session.target
PartOf=graphical-session.target

[Service]
Type=simple
ExecStart=%h/plugins/codex-show-latex-mcp/scripts/show_latex_viewer.py
Restart=on-failure
RestartSec=2
Environment=PYTHONUNBUFFERED=1
NoNewPrivileges=true

[Install]
WantedBy=default.target
